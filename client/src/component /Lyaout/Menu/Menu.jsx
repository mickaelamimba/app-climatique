import React from 'react';
import {Nav} from "react-bootstrap";
import MenuItem from "./MenuItem";

const Menu = () => {
    return (
        <Nav variant="tabs">
            <MenuItem/>
            
        </Nav>
    );
};

export default Menu;